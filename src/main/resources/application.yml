spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kids_academy
    username: root
    password: root
  server:
    port: 8080

  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath*:db/change-log.xml